import 'package:flutter/material.dart';

class QuickReplyItem extends StatelessWidget {

  const QuickReplyItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
