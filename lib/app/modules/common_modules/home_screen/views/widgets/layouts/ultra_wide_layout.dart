import 'package:flutter/material.dart';

class UltraWideLayout extends StatelessWidget {
  const UltraWideLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Ultra Wide Layout')
      ],
    );
  }
}
